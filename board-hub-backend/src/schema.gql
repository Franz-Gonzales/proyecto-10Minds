# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  createdAt: DateTime!
  deletedAt: DateTime
  description: String!
  icon: String!
  id: ID!
  isActive: Boolean!
  name: String!
  updatedAt: DateTime!
}

type Client {
  """Number of active loans (RESERVED + OVERDUE)"""
  activeLoans: Int
  ci: String!
  createdAt: DateTime!
  deletedAt: DateTime
  direction: String
  email: String!
  id: ID!
  isActive: Boolean!
  lastName: String!
  name: String!
  phoneNumber: String!

  """Total historic loans count"""
  totalHistoric: Int
  updatedAt: DateTime!
}

input CreateCategoryInput {
  description: String!
  icon: String!
  name: String!
}

input CreateClientInput {
  ci: String!
  direction: String
  email: String!
  lastName: String!
  name: String!
  phoneNumber: String!
}

input CreateGameInput {
  categoryId: ID!
  description: String
  durationMinutes: Int!
  imageUrl: String
  maxPlayers: Int!
  minPlayers: Int!
  pricePerDay: Float!
  stockTotal: Int!
  title: String!
}

input CreateLoanInput {
  clientId: ID!
  endDate: DateTime!
  gameId: ID!
  notes: String
  quantity: Int!
  startDate: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Game {
  category: Category
  categoryId: ID!
  createdAt: DateTime!
  description: String
  durationMinutes: Int!
  id: ID!
  imageUrl: String
  isDeleted: Boolean!
  maxPlayers: Int!
  minPlayers: Int!
  pricePerDay: Float!
  stockAvailable: Int!
  stockTotal: Int!
  title: String!
  updatedAt: DateTime!
}

type Loan {
  client: Client
  clientId: ID!
  createdAt: DateTime!
  deletedAt: DateTime
  deliveryDate: DateTime
  endDate: DateTime!
  game: Game
  gameId: ID!
  id: ID!
  isDeleted: Boolean!
  notes: String
  pricePerDay: Float!
  quantity: Int!
  startDate: DateTime!
  status: LoanStatus!
  totalPrice: Float!
  updatedAt: DateTime!
}

"""Status of a loan"""
enum LoanStatus {
  DELIVERED
  LOANED
  OVERDUE
  RESERVED
}

type Mutation {
  checkOverdueLoans: Int!
  createCategory(createCategoryInput: CreateCategoryInput!): Category!
  createClient(createClientInput: CreateClientInput!): Client!
  createGame(createGameInput: CreateGameInput!): Game!
  createLoan(createLoanInput: CreateLoanInput!): Loan!
  deleteGame(id: ID!): Boolean!
  removeCategory(id: ID!): Boolean!
  removeClient(id: ID!): Boolean!
  removeLoan(id: ID!): Boolean!
  returnLoan(id: ID!): Loan!

  """
  Reverts a DELIVERED loan back to RESERVED or OVERDUE. Decreases stock again.
  """
  revertLoan(id: ID!): Loan!
  updateCategory(updateCategoryInput: UpdateCategoryInput!): Category!
  updateClient(updateClientInput: UpdateClientInput!): Client!
  updateGame(updateGameInput: UpdateGameInput!): Game!
  updateLoan(updateLoanInput: UpdateLoanInput!): Loan!
}

type Query {
  categories: [Category!]!
  category(id: ID!): Category!
  client(id: ID!): Client!
  clients: [Client!]!
  game(id: ID!): Game!
  games(categoryId: ID): [Game!]!
  loan(id: ID!): Loan!

  """Get all loans. Supports filters by status, client, and game."""
  loans(
    """Filter by client ID"""
    clientId: ID

    """Filter by game ID"""
    gameId: ID

    """Whether to include deleted loans"""
    includeDeleted: Boolean = true

    """Filter by loan status"""
    status: LoanStatus
  ): [Loan!]!
}

input UpdateCategoryInput {
  description: String
  icon: String
  id: ID!
  name: String
}

input UpdateClientInput {
  ci: String
  direction: String
  email: String
  id: ID!
  lastName: String
  name: String
  phoneNumber: String
}

input UpdateGameInput {
  categoryId: ID
  description: String
  durationMinutes: Int
  id: ID!
  imageUrl: String
  maxPlayers: Int
  minPlayers: Int
  pricePerDay: Float
  stockTotal: Int
  title: String
}

input UpdateLoanInput {
  clientId: ID
  endDate: DateTime
  gameId: ID
  id: ID!
  notes: String
  quantity: Int
  startDate: DateTime
}